import { DynaPuff, Lora, Montserrat } from 'next/font/google'

export const dynaPuff = DynaPuff({
  subsets: ['latin'],
  weight: ['500'],
})

export const lora = Lora({
  subsets: ['latin'],
})

export const montserrat = Montserrat({
  subsets: ['latin'],
})
